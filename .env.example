# Template only. The application reads these values from the operating-system
# environment. Never commit a real .env file or paste the key into source code.
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=openai/gpt-oss-20b
USE_LLM=false
LLM_BATCH_SIZE=20
LLM_MAX_RETRIES=1
